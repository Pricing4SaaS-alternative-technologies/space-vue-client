# space-vue-client
Repositorio de código donde se generarán las operaciones y componentes necesarios para limitar el acceso a características del plan de precios.

Comandos para ejecutar el proyecto:

1. npm install para instalar los paquetes necesarios para el funcionamiento de la librería.
2. npm run build para que se cree la carpeta /dist para su exportación.